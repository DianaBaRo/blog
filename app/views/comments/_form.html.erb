<%= form_for(@comment) do |f| %>
  <div>
    <%= f.label :content %>
    <%= f.text_area :content, size: "24x6", :class => "form-control" %><br>
  </div>
  <button type=f.submit>Post</button>
<% end %>